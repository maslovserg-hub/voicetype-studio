"""Prompts shared across all LLM providers.

Lifted verbatim from ``transcription-bot/bot/services/summarizer.py`` so the
desktop window and the Telegram bot keep producing identical output for the
same transcript. If you tune them, do so in this single place.
"""

from __future__ import annotations

from .base import SummaryMode

SYSTEM_PROMPT = (
    "Ты обрабатываешь русскоязычные транскрипции аудио и видео. "
    "Не используй веб-поиск, не добавляй ссылок и сносок: работай ТОЛЬКО "
    "с текстом, который пользователь прислал в сообщении. Никаких '[1]', "
    "'согласно источнику', никаких внешних фактов."
)


PROMPTS: dict[SummaryMode, str] = {
    SummaryMode.BRIEF: (
        "Ты помощник, который делает краткое саммари по русскоязычной транскрипции аудио или видео.\n"
        "Структурируй ответ так:\n"
        "## Главные тезисы\n"
        "(3–7 пунктов, в каждом 1–2 предложения)\n\n"
        "## Ключевые мысли\n"
        "(развёрнутее, 3–5 пунктов)\n\n"
        "## Цитаты\n"
        "(1–3 точные цитаты из текста, в кавычках)\n\n"
        "Никаких вводных слов, ссылок и поиска по интернету — только саммари по присланному тексту.\n\n"
        "Транскрипция:\n\n{text}"
    ),
    SummaryMode.STRUCTURED: (
        "Ты помощник, который структурирует русскоязычную транскрипцию в читаемый конспект.\n"
        "Правила:\n"
        "1. НЕ выбрасывай содержание — сохраняй авторскую речь.\n"
        "2. Разбей текст на тематические разделы с заголовками формата '## Заголовок'.\n"
        "3. Внутри раздела — нормальные абзацы, можно слегка причёсывать пунктуацию и обрывки слов.\n"
        "4. Выделяй ключевые термины, имена, числа и важные понятия жирным через **двойные звёздочки** (Markdown).\n"
        "   Не злоупотребляй: 2–5 выделений на раздел, только реально опорные слова.\n"
        "5. Не добавляй ничего, чего нет в исходнике (никаких комментариев, ссылок, выводов).\n\n"
        "Транскрипция:\n\n{text}"
    ),
    SummaryMode.ROLES: (
        "Ты помощник, который размечает русскоязычную транскрипцию по говорящим.\n"
        "Если в тексте явный диалог — расставь 'Спикер 1: ...', 'Спикер 2: ...' и т.д., сохраняя порядок реплик.\n"
        "Если это монолог одного человека — верни ровно строку: 'Это монолог одного спикера, разметка не нужна.'\n"
        "Не добавляй ничего лишнего. Не ищи в интернете.\n\n"
        "Транскрипция:\n\n{text}"
    ),
    SummaryMode.QUESTIONS: (
        "Ты помощник, который ищет в русскоязычной транскрипции аудио/видео все ВОПРОСЫ.\n"
        "На вход подан транскрипт с таймкодами вида '[HH:MM:SS] текст'.\n"
        "Задача:\n"
        "1. Найди все реплики, содержащие вопрос (прямой, риторический, наводящий).\n"
        "2. Для каждого верни строку формата: 'HH:MM:SS — <вопрос>'.\n"
        "3. Сохраняй порядок появления в записи. Если ничего похожего на вопрос нет — верни ровно: 'Вопросов в записи не обнаружено.'\n"
        "Не выдумывай вопросы, бери только то что есть в тексте. Не ищи в интернете.\n\n"
        "Транскрипция:\n\n{text}"
    ),
}


def build_user_prompt(mode: SummaryMode, transcript: str) -> str:
    """Format the per-mode template with the transcript text."""
    return PROMPTS[mode].format(text=transcript)
